(env
  (_ (flags (:standard -w -27 -open Tjr_lib -open Tjr_monad -open Tjr_fs_shared))))

(library
  (public_name "tjr_plist_examples")
  (preprocess (pps ppx_deriving_yojson ppx_bin_prot ppx_jane ppx_show))
  (libraries tjr_plist ppx_show)
)

